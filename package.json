{
  "name": "zevium",
  "private": true,
  "packageManager": "pnpm@11.8.0",
  "scripts": {
    "dev": "concurrently --kill-others-on-fail --names web,convex,gateway --prefix-colors blue,magenta,green \"pnpm --filter web dev\" \"pnpm exec convex dev\" \"pnpm --filter @zevium/gateway dev\"",
    "build": "node scripts/build.mjs",
    "ci:pr": "prettier --check . && pnpm typecheck && pnpm test",
    "test": "turbo test && pnpm test:convex && pnpm test:e2e-harness && pnpm test:build-contract",
    "test:build-contract": "node --test scripts/build-contract.test.mjs",
    "test:convex": "vitest run --config convex/vitest.config.ts",
    "test:e2e-harness": "node --test e2e/harness.test.mjs",
    "typecheck": "turbo typecheck",
    "lint": "turbo lint",
    "format": "prettier --write .",
    "seed": "node --experimental-strip-types scripts/seed.ts",
    "e2e": "bash e2e/run-all.sh"
  },
  "devDependencies": {
    "@clerk/backend": "^3.14.0",
    "@edge-runtime/vm": "^5.0.0",
    "concurrently": "^10.0.4",
    "convex-test": "^0.0.54",
    "prettier": "^3.6.0",
    "tsx": "^4.23.1",
    "turbo": "^2.6.0",
    "typescript": "^5.9.0",
    "vitest": "^4.1.10"
  },
  "dependencies": {
    "@zevium/shared": "workspace:*",
    "convex": "^1.42.3",
    "stripe": "22.3.1",
    "svix": "^1.96.1"
  },
  "type": "module"
}
