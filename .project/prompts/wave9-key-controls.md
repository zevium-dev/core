Task: per-key spend limits, enable/disable, rotation with grace. Write scope: convex/key-settings.ts (new) + convex tests, apps/gateway/src/** + gateway tests, apps/web/src/routes/app/settings/keys.tsx + apps/web/src/lib/api-keys.ts + web lib helpers/tests. Do NOT touch convex/schema.ts (keySettings table ALREADY EXISTS — read it), billing, projects, catalogue.

Read first: AGENTS.md (gateway hot-path budget: no per-request Convex calls), TECH.md, convex/schema.ts keySettings table, apps/web/src/lib/api-keys.ts (Clerk machine keys: createKey stamps claims.org_id, one-key rule, revoke), apps/gateway/src/key-verifier.ts (edge cache), apps/gateway/src/wallet.ts (DO; reserve path; syncGrants pull pattern; /wallet-grants convex http endpoint in convex/http.ts).

DESIGN (locked):

1. convex/key-settings.ts: getForOrg query (auth member) → settings rows; setCap mutation {keyId, monthlyCapCredits?|null}, setDisabled {keyId, disabled} — auth: member of org owning the key (keyId must belong to caller org — verify via existing key listing or store clerkOrgId at creation; keySettings row upserted keyed by keyId). rotateKey is a WEB server-fn concern (Clerk API) — convex only stores rotatedFromKeyId + graceUntil via internal mutation called from web server fn? NO — simpler: web server fn does Clerk create+meta, then calls a public convex mutation recordRotation {oldKeyId, newKeyId, graceUntil} (auth member).
2. Gateway enforcement: extend the /wallet-grants http response (convex/http.ts — you MAY edit convex/http.ts) to include keySettings for the org: [{keyId, monthlyCapCredits, disabled, graceUntil, rotatedFromKeyId}]. Wallet DO stores them on syncGrants AND lazily refreshes if data older than 60s when a reserve arrives for an unknown keyId (single fetch, never per-request steady-state). Reserve path: reject when key disabled (403 key_disabled) or when key's month-to-date settled credits (DO already tracks per-key free-tier usage; add per-key monthly settled counter keyed keyId+utcMonth) >= cap (403 key_cap_exceeded). Old rotated key allowed until graceUntil then treated as disabled. Machine-readable JSON errors, human message too.
3. Web keys screen: per-key controls — monthly cap input (credits, blank=unlimited), enable/disable switch, Rotate button (confirm dialog: creates new key via server fn, shows new secret ONCE, old key works for 24h grace). Show cap usage if available (skip live meter if gateway state not queryable from web — display cap only). isPending, toasts, semantic tokens.
   TESTS: convex (auth, upsert semantics, rotation record), gateway (disabled reject, cap reject at boundary, grace expiry, unknown-key lazy refresh single-flight), web helpers (cap parse/format).
   VERIFY: pnpm typecheck && pnpm test green at root; npx convex dev --once clean.
   Output: CHANGED list, VERIFY results, DONE or BLOCKED.
