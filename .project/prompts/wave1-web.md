WAVE 1 — WEB LANE. Project: /home/tnfssc/Code/zevium (pnpm monorepo). First read AGENTS.md, FLOW.md, DESIGN.md, TECH.md, .project/PLAN.md at repo root. Reference working Convex+Start SSR spike code at /tmp/claude-1000/-home-tnfssc-Code-zevium/1f8a46cc-bcd1-46ef-ad97-920f53218d10/scratchpad/spikes/convex-ssr (read its src/router.tsx and routes). You may edit ONLY: apps/web/ (and run pnpm commands there). Clerk is already wired (src/start.ts middleware, ClerkProvider in __root, sign-in/up routes, keys in apps/web/.env.local). Convex url: CONVEX_URL in root .env.local — copy value into apps/web/.env.local as VITE_CONVEX_URL.

GOAL: design system foundation + app shell.

1. shadcn/ui: `pnpm dlx shadcn@latest init` (style new-york, base neutral, CSS variables) then add components: button card input label dialog dropdown-menu avatar badge separator skeleton sonner tooltip sheet tabs sidebar breadcrumb. Tailwind v4 CSS-first. Keep tokens STOCK per DESIGN.md.
2. Motion system per DESIGN.md exactly: src/lib/motion.ts exporting EASE [0.16,1,0.3,1], DUR {instant .15, fast .25, base .35, page .4, slow .6}, STAGGER 0.05, DIST 16, SPRING configs. CSS vars --ease/--dur-* in styles.css. Install motion (pnpm add motion), wrap app in LazyMotion domAnimation. src/lib/vt.ts vtState flag per DESIGN.md. Router: defaultViewTransition with types callback setting vtState (copy pattern from DESIGN.md §Route transitions).
3. Convex wiring: pnpm add convex @convex-dev/react-query @tanstack/react-query; router.tsx: ConvexQueryClient + QueryClient + setupRouterSsrQueryIntegration + ConvexProviderWithClerk (useAuth from Clerk) — follow spike router.tsx but add Clerk auth binding (convex/react-clerk).
4. App shell per FLOW.md §7: route /app (protected: beforeLoad auth check via createServerFn + auth() from @clerk/tanstack-react-start/server, redirect to /sign-in): collapsible sidebar (shadcn sidebar) with org switcher (Clerk OrganizationSwitcher themed via @clerk/ui shadcn theme — pnpm add @clerk/ui, appearance theme shadcn, import '@clerk/ui/themes/shadcn.css'), nav: Dashboard /app, Catalogue /catalogue, Projects /app/projects, Settings /app/settings; theme toggle (light/dark class on html, localStorage, instant swap, icon rotate per DESIGN.md); UserButton bottom. Breadcrumb header bar. Outlet.
5. Route skeletons (placeholder content, layout-stable, stock components): /app/index (dashboard placeholder cards), /catalogue (public, placeholder grid), /app/settings/index. On /app mount call Convex mutations ensureUser + ensureOrganization (they will exist from convex lane — if convex/_generated api lacks them yet, wire optimistically behind try/catch and note it).
6. Landing / route: minimal stock hero per FLOW.md 1.1 — headline, subline, CTA → /catalogue, sign-in button top right. Staggered entrance with motion tokens. NO magicui, NO raw colors.
7. `pnpm --filter web build` and typecheck must pass. Do not run dev server.

End final message with `DONE: <summary + files>` or `BLOCKED: <reason>`.
