Task: sweep + fix stale documentation. Write scope: PRODUCT.md, FLOW.md, TECH.md, DESIGN.md, CLAUDE.md (only the "Status" + summary sections), docs/ (NOT docs/product-discovery-2026.md — historical record, leave), apps/web/src/routes/docs/*.tsx ONLY if factually wrong. Do NOT touch README.md (orchestrator owns it), .project/, any other code.

Doc discipline (from CLAUDE.md): product language in PRODUCT/FLOW, tech in TECH only. Keep that.

The app is BUILT now. Docs were written as target-state before the build; most is accurate, but verify each claim against code and fix drift. Known reality to reconcile (verify each in code before writing):

1. CLAUDE.md "Status: greenfield rebuild" section says legacy is being replaced — it IS replaced. Rewrite that section: built, waves 1-9 done, legacy deleted. Keep target-stack summary + rules (verify rules still match reality: e.g. mock route is keyless — the "no unmetered execution paths" rule needs a stated carve-out: /mock serves synthesized responses, never executes upstream, 0 credits, keyless by design).
2. TECH.md — check it records these implemented decisions; add succinctly where missing (it owns implementation): usage ingest pipe (gateway wallet DO alarm → POST {convex-site}/ingest-usage with x-internal-secret; no deploy key), cross-org metering (consumer org wallet pays; private projects 404 to foreign keys), gateway CORS (wildcard, bearer-key auth so safe), per-key caps/rotation enforced in wallet DO (settings ride /wallet-grants pull, ≤60s staleness), Polar manual sync (billing.syncWithPolar, 5-min cooldown, dual-plane reconcile via /wallet/:org/sync-grants), semantic search (gemini-embedding-001 @ 768 dims, specEmbeddings vectorIndex — NOT text-embedding-004, that model is dead), webhooks (HMAC-SHA256 x-zevium-signature, 3 attempts 60s/300s), deprecation (RFC 8594 headers: Deprecation @seconds, Sunset HTTP-date, Link rel=deprecation), admin gate (ADMIN_USER_IDS env), payouts (manual ledger MVP: payoutRequests + /admin queue, min 100k credits), x402 stub (402 + actions envelope on keyless /gateway).
3. FLOW.md — mark implemented-variant notes where reality deviated deliberately: org screens live under /app/org (active-org model) not /app/organizations/{slug}; mock mode is keyless+anonymous; docs live in-app at /docs. Do NOT rewrite FLOW wholesale — add a short "implementation notes" line where a section deviates, or adjust the section text minimally.
4. PRODUCT.md — verify pricing/product claims still true (credits math, 95/5, mock free, payout minimum $10 if pricing belongs here). Product language only.
5. apps/web/src/routes/docs/*.tsx — spot-check factual claims (mock keyless yes; org auto-created on signup — Clerk automatic_organization_creation is now ENABLED, so quickstart "personal org created automatically" is now TRUE; verify no other stale claims).

Method: for every doc claim you change, cite the code file you verified against in the commit-ready text? No — keep docs clean; verify silently. Precision over volume: fix drift, don't rewrite style.
VERIFY: pnpm --filter web typecheck && build green if docs routes touched; otherwise state no code touched.
Output: per-file list of what changed and why (one line each), VERIFY, DONE or BLOCKED.
