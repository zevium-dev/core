Task: manual "Sync purchases" button — reconcile Polar paid orders → Convex credit ledger → gateway edge wallet. Write scope: convex/billing.ts, convex/wallets.ts, convex/http.ts, convex/schema.ts (one field/table for cooldown), convex tests, apps/gateway/src/wallet.ts + index.ts + gateway tests, apps/web/src/routes/app/billing.tsx + apps/web/src/lib/ helpers/tests.

Read first: AGENTS.md (product rules: ledger pattern, idempotent mutations), TECH.md, convex/billing.ts (createCheckout: metadata {clerkOrgId, packId, credits}, externalCustomerId=clerkOrgId, POLAR_ACCESS_TOKEN/POLAR_SERVER in convex env), convex/http.ts (/polar-webhook grants refId polar:order:{id}; /ingest-usage secret pattern), convex/wallets.ts (grantCredits internalMutation idempotent by refId), apps/gateway/src/wallet.ts (DO; /internal/grant exists; ingest client pattern in usage.ts).

CONTEXT/GAPS: Polar webhook not registered in dev (user action pending) AND nothing propagates grants to the gateway wallet DO — purchased credits would never be spendable at the edge. This feature fixes both via user-triggered sync.

BUILD:

1. convex action billing.syncWithPolar args {orgSlug}: auth = org member (existing pattern). Server-side cooldown: lastPolarSyncAt stored on wallets doc (add optional field to schema) — if < 5 min ago, return {ok:false, retryAfterSeconds} WITHOUT calling Polar. Otherwise: Polar SDK list orders (server=POLAR_SERVER) filtered by externalCustomerId=clerkOrgId, status paid; for each, resolve credits (order metadata credits, fallback product metadata — reuse webhook's resolution logic, extract shared helper), runMutation internal.wallets.grantCredits {clerkOrgId, amount, grantRefId: `polar:order:{id}`} (already idempotent — skipped if refId exists). Update lastPolarSyncAt. Return {ok:true, ordersSeen, granted, creditsGranted, balance}.
2. convex/http.ts: GET /wallet-grants?clerkOrgId=… gated by x-internal-secret === GATEWAY_INTERNAL_SECRET (same as /ingest-usage). Returns all grant-kind walletEntries for that org: [{refId, amount}] + balance. Bounded (cap 500 newest).
3. Gateway: wallet DO new op syncGrants — fetches {CONVEX_SITE_URL|derived}/wallet-grants for its org with x-internal-secret, applies any grant refIds it has not seen (DO must track applied grant refIds — check existing /internal/grant storage; reuse its idempotency store), updates balance. Route: POST /wallet/:orgId/sync-grants on the worker (no client auth needed — idempotent pull; DO-side rate limit 1/60s per org, return 429 {retryAfterSeconds} inside window). Never blocks other ops.
4. apps/web billing.tsx: "Sync purchases" outline button next to Buy credits header. onClick → convex action via useAction/mutation pattern (.mutate style, isPending spinner). On convex success ALSO fire POST {VITE_GATEWAY_URL}/wallet/{clerkOrgId}/sync-grants (fire-and-forget fetch, ignore failure silently — gateway may be down in dev). Toasts: "+N credits from M orders" / "Already up to date" / cooldown "Try again in Xs". Client cooldown: disable button with countdown using retryAfterSeconds from server (extract countdown formatting into lib helper + test). No raw colors, motion tokens, human-readable errors.

TESTS (mandatory): convex — syncWithPolar cooldown reject (no Polar call), grant idempotency via injected orders (mock Polar client boundary — extract fetchPaidOrders so tests inject), /wallet-grants auth + shape; gateway — syncGrants applies only unseen refIds, rate limit 429, ignores convex failure gracefully; web — countdown helper test.

VERIFY: pnpm typecheck && pnpm test green at root; npx convex dev --once clean.
Output: CHANGED list, VERIFY results, DONE or BLOCKED.
