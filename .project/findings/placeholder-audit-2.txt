Audit done. No file edits. Grep + every route under `apps/web/src/routes/` + FLOW.md compared.

## Grep noise vs real incompletes

**Real incomplete markers**
- `apps/web/src/routes/app/billing.tsx:168` — `"Coming soon"` usage cycle card
- `apps/web/src/routes/app/projects/$projectSlug.tsx:278` — `toast.message("Settings coming soon")`
- `apps/web/src/routes/app/settings/activity.tsx:19-23,50-51` — explicit call-log placeholder; no query wire

**Not product stubs**
- Input `placeholder=` props, CSS `placeholder:` classes
- Gateway `DurableObjectStub` + wrangler domain comment
- `convex/`: zero TODO/FIXME/coming-soon

---

## Route judgment (real vs shell)

| Path | File | Verdict |
|---|---|---|
| `/` | `routes/index.tsx` | **Mostly real** — Convex `listPublic` teasers. Empty → static `FALLBACK_TEASERS` (non-clickable fake cards). Stats = listed count + hard `95%`, no calls-served. No Docs nav/footer. |
| `/catalogue` | `catalogue/index.tsx` | **Real list** — search = substring filter, tag chips. Missing FLOW: semantic search, sort, price/free-tier filters, quality badges, price range on cards. |
| `/catalogue/$org/$project` | `catalogue/$orgSlug.$projectSlug.tsx` | **Real core** — pricing from spec, live Try-it `fetch` gateway, key in sessionStorage. Docs = endpoint list only (no 3-col prose/code/hover-sync). Agent tab = static MCP JSON + notes (hardcoded `https://gateway.zevium.dev/mcp`). No mock mode, version picker, reviews, quality badges. |
| `/sign-in/$`, `/sign-up/$` | Clerk prebuilt | **Real** (Clerk hosts 2FA/reset/OAuth). |
| `/app` | `app/index.tsx` | **Real** — `analytics.orgOverview`, keys count, recent calls. Onboarding top-up step hard `done: false` always. |
| `/app/billing` | `app/billing.tsx` | **Half real** — wallet, Polar packs/checkout, grant/ledger. Usage card **Coming soon**. No per-member/key/endpoint breakdown, no charge→call links, no spend budgets. Path not org-scoped URL. |
| `/app/settings` | `settings/index.tsx` | **Thin shell** — read-only Clerk name/email + link to keys. No edit forms, no prefs. User report matches: settings = not product prefs surface. |
| `/app/settings/keys` | `settings/keys.tsx` | **Real CRUD** — list/create/revoke (1 key/user). Missing FLOW P1: spend limits, sparklines, enable/disable, rotation. |
| `/app/settings/activity` | `settings/activity.tsx` | **Placeholder** — dashed empty only; admits no public listUsage. Dashboard already has recent via `orgOverview`. |
| `/app/projects` | `projects/index.tsx` | **Real** — Convex list + empty CTA. |
| `/app/projects/create` | `projects/create.tsx` | **Real** — `projects.create` submits. |
| `/app/projects/$slug` | `$projectSlug.tsx` | **Real overview + analytics** — visibility mutate, `projectAnalytics`. **Settings tab dead** (toast). No Earnings tab. Overview no edit name/tags/description UI. |
| `/app/projects/$slug/spec` | `spec.tsx` | **Real** — draft save/publish/semver/issues/versions list. Missing: URL/file import, YAML path, live validate-as-type, version diff/rollback, upstream creds (settings). |
| Auth layout `/app` | `app.tsx` | **Real** shell + Clerk gate. |

**Dead / weak actions**
- Project **Settings** tab → toast only, no navigate, no form
- Billing **Usage this cycle** card → static text, no CTA
- Activity page → no filters, no data, only catalogue link
- Settings profile → display only; password/2FA deferred to Clerk avatar menu (ok) but FLOW prefs route missing
- Onboarding “Top up” never completes even after wallet balance > 0
- Catalogue empty fallback teasers not links
- Connect-agent MCP host may desync from `VITE_GATEWAY_URL`

**Backend gaps that force shells**
- No public `listUsage` (activity says so); `recordUsage` internal only
- No usage rollup for billing breakdown (UI blames “usage rollup cron”)
- No earnings/payouts/members/invites/admin tables or functions
- Catalogue search not vector/semantic (comment: later vectorIndex)
- `projects.remove` exists, **no UI**

---

PLACEHOLDERS:
- `/app/settings` — `settings/index.tsx:42-96` — read-only profile + keys shortcut; no theme/notification prefs, no org profile — needs prefs model + `/app/settings/preferences` (or expand this) and product decision: Clerk UserProfile embed vs custom
- `/app/settings/activity` — `activity.tsx:19-61` — full call-log placeholder — needs public org-scoped `listUsage` (filters, pagination, indexes on `usageEvents.at`)
- `/app/billing` usage card — `billing.tsx:162-174` — “Coming soon” projected/per-key/per-endpoint — needs usage rollup (cron or analytics query by key/API/endpoint) + product cycle definition (already UTC month on dashboard)
- `/app/projects/$projectSlug` Settings tab — `$projectSlug.tsx:277-286` — dead toast — needs project settings UI: description/tags, upstream encrypted creds, danger zone; schema for secrets + encrypt path (TECH)
- Catalogue cards — `catalogue/index.tsx:163-214` — no price range / quality / agent-ready badges — needs listPublic fields from latest spec + quality metrics pipeline
- Catalogue search/filters — `catalogue.ts:43-44` + UI — substring only, no sort/price/free-tier — needs searchIndex/vector + filter args
- API detail Docs tab — `$orgSlug.$projectSlug.tsx:389-431` — endpoint list not 3-col docs — needs OpenAPI→prose renderer + multi-lang samples + hover-sync
- API detail missing mock/version/reviews — same file tabs — mock mode (P1), version picker/diff (P2), ratings (P2)
- Connect agent MCP URL — `$orgSlug.$projectSlug.tsx:767` — hardcoded prod host — needs env-driven gateway base
- Landing FALLBACK_TEASERS — `index.tsx:22-45,228-235` — static non-linked demos when empty — product: seed demos vs hide strip
- Landing header/footer Docs — `public-header.tsx:43-53`, `index.tsx:309-326` — Docs link missing entirely — needs docs site/route decision
- Keys P1 controls — `keys.tsx` table — no spend limit/sparkline/enable/disable/rotation — needs key metadata store + gateway enforce
- Spec editor import — `spec.tsx` — paste-only textarea — needs file upload + import-from-URL + YAML accept path
- Project overview edit — `$projectSlug.tsx:306-358` — display only — needs form wired to `projects.update` (mutation exists)
- Delete project — `convex/projects.ts` `remove` — no UI danger zone
- Onboarding top-up done — `app/index.tsx:279-286` — hard `done: false` — needs balance/grant signal
- Billing charge→call links — ledger shows `refId` only — needs join usageEvents ↔ walletEntries
- Org create/settings/members/invites — no routes — relies on Clerk OrganizationSwitcher only; FLOW wants dedicated screens
- Org home / earnings / admin — no routes — missing whole surfaces
- Preferences — no `/app/settings/preferences` route
- Publisher webhooks / deprecation UX / quality surface — no UI
- Spend budgets/alerts — no UI
- Platform `/admin` — no route tree

MISSING VS FLOW.md:
- **1.1** Docs nav; calls-served live stat; full footer
- **1.2** Semantic search; sort; price/free-tier filters; quality + agent-ready badges; price range on cards
- **1.3** Quality badges; full docs chrome; mock mode (P1); version picker/diff (P2); reviews (P2); richer agent multi-client configs
- **1.4** Post-auth onboarding checklist completeness (top-up never done); password/2FA UX is Clerk-only (acceptable if intentional)
- **2.2** Per-key spend limits/reset/auto-disable (P1); rotation grace (P1); key-management API (P1); sparklines; enable/disable
- **2.3** Path is `/app/billing` not `/app/organizations/{org}/billing`; projected + breakdown card stub; itemized charges→call; spend controls (P1)
- **2.4** Activity page stub (dashboard has partial recent)
- **2.5** `/app/settings/preferences` missing
- **2.6** Integrate snippets partial (curl on try-it only); generated SDKs (P2)
- **3.1** Discovery exists on gateway (`GET /discovery`) — not web; OK machine surface
- **3.4** x402 (P1) — no surface
- **4.1** `/app/organizations/create` missing (Clerk switcher only)
- **4.2** Org home `/app/organizations/{org}` missing
- **4.3–4.4** Projects live at `/app/projects*` (path drift from FLOW, function exists)
- **4.5** Project Settings tab stub; no Earnings tab; no upstream credentials; no danger zone
- **4.6** Import URL/file; YAML dual; version diff/rollback (P2); richer live lint-as-type
- **4.7** Analytics partial real — missing consumers/retention breakdown, revenue-as-money narrative
- **4.8** Earnings & payouts (P2) missing
- **4.9** Deprecate/unpublish sunset flow (P1); quality surface (P2)
- **4.10** Publisher webhooks (P1) missing
- **5.1** Org settings members/roles/invite/danger/wallet admin missing
- **5.2** `/app/invitations` missing
- **5.3** Choose-org dedicated page thin (empty states only)
- **6** Entire `/admin` platform admin missing
- **7** In-app notifications system; invitation/budget/deprecation emails — not built as product UI

DONE.
